<Window x:Class="BioAlgorithm.Core.Representatives.DeleteAlgorithmInputWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:BioAlgorithm.Core.Representatives"
        mc:Ignorable="d"
        Title="Delete" Height="250" Width="700"
        Loaded="Window_Loaded"
        WindowStartupLocation="CenterScreen">
    <Grid Background="#EEF2EE">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Label Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2" Margin="5" FontSize="16" HorizontalAlignment="Center" Content="{Binding Title}"/>
        <TextBlock  Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2" Margin="15" FontSize="14" TextWrapping="Wrap" Text="{Binding Content}"/>
        <Button Grid.Row="2" Grid.Column="0" FontSize="16" Padding="3" Command="{Binding DeleteCommand}">Delete</Button>
        <Button Grid.Row="2" Grid.Column="1" FontSize="16" Padding="3" Command="{Binding CancelCommand}">Cancel</Button>
        <Label Grid.Row="3" Grid.Column="0" Grid.ColumnSpan="2" Margin="5" FontSize="16" Content="{Binding ExecutionState}"/>
    </Grid>
</Window>
